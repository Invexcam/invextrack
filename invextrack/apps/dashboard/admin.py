# Importer des modules spécifiques
from .tasks import models as task_models
from .objectives import models as objective_models
from .projects import models as project_models
from .dashboard import models as dashboard_models
from .support import models as support_models
from .settings import models as settings_models

# Initialisations spécifiques au package
# Vous pouvez ajouter du code ici si nécessaire